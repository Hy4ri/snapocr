[package]
name = "snapocr"
version = "0.2.0"
edition = "2021"
description = "Lean & fast screen OCR to clipboard for Wayland & X11"

[dependencies]
arboard = { version = "3.6", features = ["wayland-data-control"] }
image = { version = "0.25", default-features = false, features = ["png", "pnm"] }
